# Reports

Dieser Ordner enthaelt menschenfreundliche HTML-Reports fuer `eventbear-worker`.

Die Inhalte hier sind Laufzeit-Artefakte, keine Produktquelle:

- `patternpilot-report-*.html`
- `latest-report.json`
- `browser-link`

Sie helfen lokal bei Review und Closeout, sollen aber normalerweise nicht nach GitHub committed werden.
