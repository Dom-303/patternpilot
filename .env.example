# Patternpilot GitHub API auth
# Prefer a fine-grained personal access token for stable API limits.
PATTERNPILOT_GITHUB_TOKEN=

# Optional fallbacks if you already use these names elsewhere.
GITHUB_TOKEN=
GITHUB_PAT=
